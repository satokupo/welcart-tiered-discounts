# 作業ログ 2026-09

> 追記専用・新しいものが上。1コミット＝1エントリ。過去のエントリは書き換えない。
> 書くのは「何を変えたか」と **なぜそう判断したか**。仕様・設計の決定そのものは `../../../SSoT/decisions/`（索引は `../../../SSoT/DECISIONS.md`）。

<!-- ここから下に追記する。新しいエントリを常にこの直下へ置く -->

## 2026-09-09 レポート統合結果を是正してdevへ反映 `bdeb2ce`

- **変更**: 競合マーカーを除去し、レポートと設定を公開済みa752f2aと完全一致させた。両作業のログを保持し、旧記録3件を取り込んだ。
- **判断**: 中間コミットの誤りを明記し、合意済みの内容だけを検証後にdevへ反映するため。
- **検証**: node docs/report/check.mjs、git diff --check、公開済み版とのdocs/report・.gitignore差分検査は終了0。merge_landing.pyはMERGED・終了0。dev..codex/submission-reportは0件。配信ファイルは不変のため再デプロイは不要。

## 2026-09-09 旧レポートブランチの履歴を接続 `6fe1348`

- **変更**: コピーで分離していたsubmission-reportの履歴を、現行report-reviewの親履歴へ接続した。
- **判断**: ユーザー指定に従い、公開内容を維持して旧制作履歴と作業記録3件を残すため。
- **未達**: 統合スクリプトの構文エラー後に実行を止められず、競合マーカーを含む中間コミットとなった。この状態はdev・公開サイトへ反映せず、次の修正コミットで是正した。

## 2026-09-09 提出レポートを公開用に整備 `d63f795`

- **変更**: 提出サイトの本文・main/devの区分・3箇所の検証根拠・操作動画2本を収録し、トップへ戻るボタンと動画の状態別カーソルを追加。不要な再生・一時停止ツールチップを除去した。
- **判断**: 人が課題要件と証拠を辿れるようにし、撮影時点で発見した未対応事象と金額不具合の修正実績を区別した。ユーザー指定に従ってdevだけへ統合し、既存Cloudflare Workerを更新する。
- **検証**: python3 docs/report/build.py、node docs/report/check.mjs、git diff --cached --check、wrangler deploy --config docs/report/wrangler.jsonc --dry-run は終了コード0。プラグイン本体の試験は今回再実行していない。

## 2026-09-09 提出サイトの公開設定を追加 `4db1b0d`

- **変更**: Workersの公開設定、X-Robots-Tagとrobots.txt、公開動画を2本に限定するassetsignoreを追加した。
- **判断**: 認証なしでURLを共有しつつ、検索掲載を拒否し、巡回規約に従うボットのアクセスを抑えるため。認証によるアクセス制限ではない。
- **検証**: wrangler deploy --dry-runとgit diff --checkは終了コード0。ローカル配信は環境制約で起動できず、配信ヘッダーと除外動画は公開後に実レスポンスで確認する。

## 2026-09-09 提出レポートと実装レビュー資料を整理 `1ac678c`

- **変更**: 提出本文の構成・説明を整理し、実装レビュー資料のHTMLと根拠資料、生成スクリプトを追加した。
- **判断**: 人間の判断と実装の根拠を読みやすくし、詳細資料を提出サイトから参照できるようにするため。
- **検証**: build.py、node docs/report/check.mjs、git diff --checkはいずれも終了コード0。

## 2026-09-09 提出用レポートと操作動画を整備 `36f60cdebf79a442147c4fe2bb6c130269b2210a`

- **変更**: 4タブの提出レポート、UI比較資料、注文と注文後編集の動画2本を追加。動画の操作・時刻・説明を表に整理し、AI活用の振り返りと未対応範囲を記載した。構造・タブ操作のチェックとステージ差分の整合確認は終了コード0。
- **判断**: 動画はユーザーが撮影・編集した buy3.mp4 と edit2.mp4 を再圧縮せず採用し、確認する順序と割引の変化を説明で補った。

## 2026-09-08 devの参考版表記と検証済み版を更新 `fedbc85`

- **変更**: READMEに主たる提出用mainと時間超過後の参考版devを明記し、追試結果を環境検証記録へ追記した。devのTested up toを7.1へ更新した。
- **判断**: 最新環境で成功したdevの検証結果をmainの実績へ読み替えず、提出先に両版の役割を伝えるため。
- **検証**: 通常構成のPlugin Checkは終了コード0・指摘なし。git diff --checkは終了コード0。

## 2026-09-08 下限・最新環境の連携試験を補完 `48f679f`

- **変更**: メール捕捉とPHPごとの警告・後始末の差異をテスト補助コードで吸収した。
- **判断**: 金額・保存保護の期待値を変えず、実際の対応下限と最新環境で同じ連携試験を走らせるため。
- **検証**: 両環境68 tests / 1,042 assertions、標準quality68 tests / 96 assertionsが終了コード0。ブラウザー購入・受注・メール本文も一致。

## 2026-09-08 native連携の採用境界と不採用根拠を記録 `453c33a`

- **変更**: INTEGRATIONSへ税APIと標準/軽減script filterを追加し、設計判断と索引を記録。#7/#8の実験結果をvisdocレポートv2へ整理。
- **判断**: native受注再計算へ割引総額だけを渡すと混合税率の内訳が同期せず、未保存IDでも税区分を取得できない。全面置換より税計算と入口だけの委譲を採る。
- **検証**: Pandoc、GFM export、HTML見出し・結果・文脈検査、git diff --checkは終了0。Plugin Checkの既知残件を新規blockerと誤認した確認は、既存README/環境記録を照合して撤回。

## 2026-09-08 税計算と再計算入口をnativeへ委譲 `b599c9f`

- **変更**: 税額・丸めをWelcart APIへ委ね、既存script filterで署名付きプレビューへ接続。受注集計全体は未保存行の反例を確認して維持。
- **判断**: 税率別の対象額を渡す方法は数式の重複を除き、cart singletonの再構築やJSON終了の封じ込めを製品へ持ち込まずに済む。
- **検証**: recommended quality終了0（68 tests / 96 assertions、PHPCS警告0）、integration終了0（68 tests / 1042 assertions）、Node応答順チェック終了0。標準/混合×税込/税別の画面・HTTP保存・税メタ・履歴を確認。独立レビュー2回とコミット後全体自己レビューは必須修正0。検証済み14ファイルとcommit内容のバイト一致を確認。
- **制約**: Plugin Checkは終了0だが既存のoutdated_tested_upto_header ERROR1。READMEの方針どおり7.1対応を表明しない。latest/minimumはnetwork address pool枯渇で起動終了1、スモーク未実行。通常runtimeへ復元しtest env/MU mountなしを確認。Eとvolumeを保持、未マージ。

## 2026-09-08 既存フック置換実験の着手とHTTP接続先を整理 `6ddd80c`

- **環境割当**: E（8480）、Compose project `welcart-tiered-discounts-e` を `codex/native-hook-experiment` へ割当。既存コンテナ・volume・networkなし、ポート未使用を確認。基準は `b0c0e1a`。A〜Dは既存作業の接続を維持。
- **依頼範囲**: #7／#8の3候補を検証し、要件と保守性を満たす部分を差し替える。コミット後の自己レビュー・修正とpushまで行い、dev／mainへはマージしない。
- **変更**: 共通HTTPクライアントと割当済みポートを用い、失敗・同時更新試験がC固定の環境へ接続しないようにした。

## 2026-09-07 IMP-01修正をmainへ反映 `2cfe355`

- **変更**: ユーザーの追加指示に従い、dev の修正・回帰テスト・ELI5資料・作業記録を PR #6 で main へマージ。GitHubの main を再取得して `2cfe355ab287f6f5512c61b5eddbff85e0589153` と照合し、ローカルmainも同期。
- **判断**: IMP-01のコードとテストは検証済みcommitから不変のため、標準品質・全体連携の成功証拠を再利用した。前の記録にあるmain未反映は、この追加指示による反映で解消。既存提出ZIPは未更新。

## 2026-09-07 初回・空一覧からの割合設定保存を修正 `c32cce3`

- **変更**: Issue #4 の IMP-01 を採用し、フォームと正規化済み設定を区別して二重の割合変換を修正。未作成／空一覧からの 0.5%・10% の HTTP 保存、内部値 50・1000、再表示・再保存、商品基準額 10,000 円への割引 50・1,000 円を回帰テストに追加。
- **判断**: WordPress 7.0.4 の update_option → add_option で再サニタイズされるため、保存形式も既存の厳密な検証へ通す。既存設定・過去注文は推測で補正しない。計画本文・仕様・データ構造は変更しない。
- **検証**: 隔離 D 環境で `./scripts/dev.sh recommended integration --filter SettingsHttpTest` が修正前 5 tests / 61 assertions / 4 failures（終了1）、修正後 5 tests / 85 assertions（終了0）。`./scripts/dev.sh recommended quality` は PHPCS/WPCS を含め終了0、68 tests / 96 assertions。`./scripts/dev.sh recommended integration` は終了0、56 tests / 839 assertions。独立コードレビューは指摘なし（レビュー担当はテストを再実行していない）。
- **反映**: dev へ安全な fast-forward landing（終了0）。main と既存提出ZIPは今回更新対象に含めない。

## 2026-09-07 検証レポートのELI5版を追加 `6b7edb0`

- **変更**: 大きな図と数量変更・再計算・保存の説明用デモを持つHTMLと元Markdownを追加。Pandoc生成、HTML構造・リンク検査、Nodeによるデモ状態遷移検査は終了コード0。
- **判断**: 元レポートの検証結果と未確認範囲を保持し、再試験や実画面の証拠とは区別した。ユーザーのコミット指定により、通常追跡外のitems内の今回2ファイルのみを追跡対象へ追加した。

## 2026-09-07 main統合後のdev着地を確認 `15a3989`

- **変更**: 保存前の着地照合でhelperが内容不変のmerge commitを生成。直前のb607f7eとのgit diff --exit-codeは0で、製品・文書の内容変更はない。mainはPR #5の61b532aを保持する。

## 2026-09-07 検証済みの段階割引をmainへ反映 `61b532a`

- **変更**: 最終の自己チェック・独立レビュー結果を確認し、ユーザー承認に基づきPR #5でdevからmainへ統合。GitHubはmerged=trueを返し、ローカルmainとdevへ統合結果を取り込んだ。
- **判断**: 製品・テストコードは検証済み93efee3以降に変更がなく、既存のquality68/96・integration52/779の証拠を再利用。補助環境と掲載審査用の制約を保持して提出可能な範囲を反映した。

## 2026-09-07 レポートの掲載用検査結果を補完 `cc840f5`

- **変更**: Plugin Check欄の仮文を、実行exit 0・outdated_tested_upto_header 1件の実測結果へ置き換えた。MarkdownとHTMLを一致させ、git diff --checkは終了コード0。

## 2026-09-07 採用仕様と提出用の検証証跡を整備 `81dd559`

- **変更**: SSoT、導入手順、TDD記録、画面証跡、実装・検証レポートを整備し、作業ブランチをdevへマージした。
- **判断**: 推奨環境の受入れ結果と補助環境の確認範囲を分離。minimumのネイティブ購入は確認済みだがHTTP・メールは未検証、latestはDB認証エラーにより未検証。Plugin Checkの掲載審査向け指摘も成功扱いせず記録した。

## 2026-09-07 段階割引と標準注文処理の連携を実装 `93efee3`

- **変更**: 管理設定、購入時割引、注文編集の再計算・履歴・競合検出を実装。標準qualityは68 tests / 96 assertions、integrationは52 tests / 779 assertionsで終了コード0。
- **判断**: Welcart標準の税・在庫・会員ポイント処理を再実装せず、補助メタと同一transactionで確定し、途中失敗時は全体を巻き戻す。

## 2026-09-07 詳細計画v4をクラウドレビューへ引き継ぎdevへ統合 `fdddb1a`

- **変更**: 詳細計画を各版の全文レビュー後に一括改訂し、Draft v4をIssue #4本文へ反映。繰越分と未検証の技術案を区別した引継ぎコメント5567053689を作成した。UI比較は幅960pxの現行画像と固定額に統一した例示を持つv4へ更新した。計画・UI資料はローカル生成物として保持し、公開済み計画の正本はIssue本文とする。
- **判断**: ユーザーの指示でローカル修正を止め、クラウドレビューへ移行。終了処理で判断索引と作業ログの追記が競合したため、明示承認後に双方の記録と失効注記をすべて保持して解消した。デモ商品側の既存変更は保持した。
- **検証**: 計画v4の全文自己レビューと独立した計画レビューは要求Finding 0件。Pandoc、Pythonによる版・参照・Issue本文とコメントの完全一致、競合両側の履歴保持、git diff --checkは終了0。merge_landing.pyはMERGED（終了0）、devへの未マージ0件。宣言済みローカルCIはなく、PHP・Composer・機能テストの変更もないためPHP品質検査は今回の文書統合には実施していない。
- **残作業**: 計画はDraft・未FIX。製品コード・機能テストは未実装で、P0の数値精度・税率別同期・保存境界などの実測とクラウドレビューが後続。環境の成功を割引機能の受入れ結果へ読み替えない。

## 2026-09-07 デモ商品画像4枚をdevへ反映 `961cda8`

- **変更**: 白背景のローポリゴン画像4枚を `docker/demo-product-images/` へ保存し、SSoTの商品画像対応表と再投入手順を追加。ユーザーの明示指定に従いdevへ直接反映した。
- **判断**: マグカップを赤、スープボウルを濃い緑、皿付きコーヒーカップを白背景でも見える黄色、カトラリーセットを紫とし、画像内の「サンプル商品１〜４」で識別する。商品コード・価格は既存定義を保持した。
- **生成条件**: 内蔵imagegenを使用。共通プロンプトは白背景、単純な面で構成した雑貨、紺色の文字と影、余白のある正方形、指定の商品名・番号。採用した赤いマグカップのテイストを参照し、商品2〜4の色のみを変更。各PNGは1,254×1,254ピクセルで、採用画像とのハッシュ一致を確認した。
- **dev適用**: A（8080）、Welcart画面表示版2.12.1.2608181。`wp media import` で代替テキスト付きの画像4枚を登録（添付ID23〜26）。新商品画像登録が有効なため、タイトル・添付先だけでは紐付かないことを実コードで確認。管理画面と同じ `wel_add_item_image_ids()` をWP-CLIから呼び、商品ID16／20／21／22の主画像へ紐付けて再取得一致を確認した。既存画像がないことを全件確認してから追加した。
- **検証**: 画像取込4件、紐付けとread-back、PythonによるCSV4商品・画像4枚・寸法・SSoT参照の照合、`git diff --check` はすべて終了コード0。商品マスター4/4件、全4商品詳細の色・画像・価格、カートの赤いマグカップと紫のカトラリー表示をブラウザで確認（UI操作の終了コードは非該当）。カート検証で追加した1円商品のみを外し、既存のカート内容へ戻した。
- **検証範囲**: プラグインコード・割引計算は変更していない。割引機能の受入れや注文確定はこの画像反映の検証へ含めない。

## 2026-09-07 デモ商品4種類とSSoTを作成 `3e8a141`

- **作業ライン**: `codex/demo-products`。分岐元 `dev` の `6093f74`。実装コミットは `3e8a141`。
- **変更**: `docs/SSoT/DEMO_PRODUCTS.md` に4種類の商品構成、価格と種類数の理由、固定額・割合割引の検証例を定義。SSoT索引・判断記録・ローカル投入手順と `docker/demo-products.csv` を揃えた。
- **判断**: ユーザーの指定に従い、SSoTは「4種類」の絶対値で記載した。商品登録の成功と割引機能の受入れを区別する。
- **環境割当**: B（8180）を本作業へ割当。B専用のCompose project、ネットワーク、WordPress volume、DB volumeを作成し、作業worktreeの `plugin/` を接続した。Bの準備時にはAのDB・コードを変更していない。自動アドレスプール枯渇に対して、重複のない専用サブネットをローカル設定へ指定した。
- **ローカル再開情報**: このworktreeの `docker/runtime/environment-b.yaml` と `docker/runtime/b.sh` がB用の設定・操作入口。認証値は `docker/local.env`、すべてGit管理外。起動は `./docker/runtime/b.sh up -d db wordpress`、停止は `./docker/runtime/b.sh stop db wordpress`。通常の `dev.sh recommended` はAへ接続するため、Bの操作に使用しない。
- **環境準備**: WordPress 7.0.4、Welcart 2.12.1.2608181、Twenty Twenty-Fiveを使用。JPY・日本向け・税抜／標準税率10%・商品代金課税を設定。送料ID 0は全国0円、配送方法ID 0は標準配送、決済方法はローカルデモ用の代金引換。実決済・注文確定は行っていない。
- **登録結果**: Welcart標準画面の全項目CSV取込で、UTF-8・カテゴリslug設定を使用。データチェック、本登録とも成功4行・失敗0行・異常なし。Bの商品IDは8／9／10／11、各1SKU・在庫99・公開状態。IDは環境内の観測値であり投入キーにはしない。
- **画面検証**: 商品一覧の4価格、税別5,000円／税込5,500円の販売表示、カート数量2個で商品小計10,000円、数量を1個へ更新すると5,000円になることを確認した。カートの小計はいずれも税別。UI操作の終了コードは非該当。
- **コマンド検証**: Compose `config --quiet` とB起動、WordPress初期化・Welcart有効化は終了コード0。`docker exec ... php` で4商品の公開状態と税抜設定を確認。Python標準csv・リンク検査で4データ行／37列／コード一意／価格一致／14ローカルリンクを確認。Pandoc生成HTMLのtitle・h1・6個のh2・商品コード・リンクを確認。各検査と `git diff --check` は終了コード0。
- **独立レビュー**: 判断記録に残った時点依存表現の指摘を修正し、修正確認PASS。データ構成と機能仕様の整合を確認した。
- **未検証**: 割引機能の適用、購入確認・受注データとの一致、ポイント併用などの割引受入れは未実施。商品準備の成功をそれらのPASSへ読み替えない。
- **dev反映確認**: A（8080）の既存基準商品を再利用し、不足3行を環境のCSV形式（Shift_JIS・カテゴリID）に合わせたコピーから登録。チェック・本登録とも成功3行／失敗0行。商品マスターは4/4件、4価格・各1SKU・在庫99・公開済みを確認。デモ仕様に合わせて消費税区分を税込から税別へ変更し、ショップで税別5,000円／税込5,500円を確認した。Aの商品IDは16／20／21／22。UI操作の終了コードは非該当。

## 2026-09-07 追加回答で価格基準・受注編集・MVP境界を訂正 `e369f96`

- **変更**: SSoTを登録販売価格・一切の減額前の小計、設定破損時の注文停止と内部ログ、注文時の全ティアによる編集再計算と元注文・変更履歴の保持へ訂正。計画をv2、UI比較を別セッションのv2からv3へ差分改訂。表示名をステップ割引に統一し、PCで再撮影、購入経路の3比較を独立した表示ブロックへ変更。
- **判断**: ユーザーの明示的な訂正を反映し、税抜換算と他割引の検出・競合制御、工数算入判定をMVP作業から外した。ポイント適用後金額による判定は後続候補とし、未使用機能は先行実装しない。
- **状態**: 仕様訂正をe369f96へ確定。製品・機能テストは未実装。親Issue #3本文v1との差異は追加回答コメント5565656949へ記録。UI比較3-1・3-2の表示幅について追加の修正依頼を受領。
- **検証**: 文書リンク74件・UI構造の検査、pandoc、git diff --checkは終了0。

## 2026-09-07 SSoT監査のR1・R2を修正 `8472d32`

- **変更**: 標準ポイント併用をMVPに含めることをQ4へ明記し、ローカル絶対パスはGit管理外の記録に限定した。起動例の固定ローカルパスを除去した。
- **判断**: 両指摘を採用し、既決仕様と記録先の境界へ文章を揃えた。製品仕様・実装コードは変更していない。
- **検証**: Pythonで参照26件、変更範囲、未確認事項の保持を確認。git diff --checkを含め終了コード0。Welcart連携・UI・機能テストは未実施。

## 2026-09-07 ローカル開発の操作手順を集約 `a57a400`

- **変更**: 初回設定、日常の起動・停止、接続確認と検証コマンドをLOCAL_DEVELOPMENT.mdへ集約し、READMEを案内リンクへ変更した。SSoTの配置ルールと判断記録も揃えた。
- **判断**: ローカル開発の情報を一つの文書で辿れるようにし、READMEとの二重管理を避けるため。dev上の変更として確定し、別ブランチからのマージは不要。
- **検証**: Pythonによる6文書・22ローカルリンク／見出しの検査、bash -nによる12シェル記述の検査、git diff --checkはいずれも終了コード0。実行環境の再起動は行っていない。

## 2026-09-07 SSoTの競合を解消してdevへ統合 `2184584`

- **変更**: 4ファイルの競合を解消し、今回のSSoT・開発ルールをdevへマージした。既存の開発運用方針、判断索引、作業ログは保持した。
- **判断**: ユーザーの競合解消・dev統合指示に従い、指定されたプライマリのdocs/SSoTへ反映した。
- **検証**: ローカルリンク67件、双方の履歴保持、競合マーカー除去、git diff --checkを確認。merge_landing.pyはMERGED（終了コード0）、未マージコミット0件。

## 2026-09-07 連携SSoTの本文を直接修正 `0d7ffe4`

- **変更**: 日付付き観測節への移動を取り消し、連携資料を確定した連携契約と公式APIの事実へ書き換えた。
- **判断**: ユーザーの指摘に従い、ログへの移動だけでは正本の修正にならない点を是正した。先の移動対応は本修正で置き換える。
- **検証**: Pythonで20文書・56リンク、暫定的な実装・稼働状態の除去を確認。git diff --checkも終了コード0。

## 2026-09-07 自己レビューで文書の配置を修正 `be5a2ed`

- **変更**: 一時的な実装状況・コンテナ稼働状況を連携SSoTから日付付きの作業記録へ移した。
- **判断**: 固定的な連携仕様と変動する観測情報の混在を解消した。確定要件との矛盾は見つからず、実コード・税・ポイント・UIの未検証事項は保持した。
- **検証**: 19文書・52ローカルリンクと判断ログ索引のPython検査、git diff --checkは終了コード0。コード変更なし、機能テスト未実施。

## 2026-09-07 段階式割引の要件と開発ルールを確定 `3f363e4`

- **変更**: 要件・MVP・機能仕様・設定構造、公式連携候補、確認事項、開発ルールと後続作業を文書化した。
- **判断**: ユーザー回答を確定事項へ反映し、対象版の実測を要する連携やUIは未確認として分離した。

## 2026-09-07 devを開発統合先へ変更 `be4602b`

- **変更**: mainの既存変更をdevへ取り込み、プライマリworktreeをdevへ切り替えた。LOCAL_DEVELOPMENT.mdと判断記録をdevで統合・検証してPRでmainへ反映する方針へ更新した。
- **判断**: 並列開発の統合先と安定版を分離し、既存のdev優先のマージ先判定とプライマリを一致させるため。

## 2026-09-07 ローカル並列開発の運用方針を定義 `1558f8c`

- **変更**: A〜Eのポート、worktreeと環境の対応、DB更新の統合方針をLOCAL_DEVELOPMENT.mdにまとめ、AGENTSの参照入口・SSoT索引・判断記録を追加した。Composeや実行環境の変更は未実施。
- **判断**: 作業間のDB干渉を避けつつ再インストールを減らすため、独立した環境を保持して再利用し、採用したDB更新だけをコードと共に各環境へ反映する。

## 2026-09-03 WordPressプラグイン開発用Skillを追加 `cd7ba3e`

- **変更**: WordPress/agent-skills 由来の wp-plugin-development を追加し、参照資料、プラグイン検出スクリプト、skills-lock.json、Claude 向けシンボリックリンクを同梱した。
