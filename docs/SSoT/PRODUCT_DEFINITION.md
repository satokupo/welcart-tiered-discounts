# プロダクト定義

**状態: 未着手** — この下は本文ではなく、この文書を書くための指示。

## AI への指示（書き終えたらこのセクションごと削除する）

下の項目をユーザーへ聴取し、回答を本文として書く。**推測で埋めない**。ユーザーが答えられない項目は `（未決）` と書いて残し、勝手に決めない。一度に全部聞かず、1〜2問ずつ確認する。

この文書は「判断に迷ったとき、そもそも何のためだったかに立ち返る」ための一枚。一度書けばほとんど変わらない性格の情報だけを置く。

### 聴取する項目

1. このプロダクトは何をするものか（1〜2文で）
2. 主な利用者は誰か（種類が複数あるならすべて）
3. その利用者の、どの困りごとを解消するか
4. どんな場面で使われるか（いつ・どこで・どんな状況で）
5. 将来どうしたいか（無ければ `（未決）` のままでよい）

### この文書に書かないもの

| 内容 | 行き先 |
|---|---|
| 個々の機能の動き | `FUNCTIONAL_SPEC.md` |
| 最初にどこまで作るかの線引き | `MVP_SCOPE.md` |
| 使う技術 | `TECH_STACK_AND_OPERATIONS.md` |

### 書き終えたら

- このセクションと冒頭の状態表示を削除し、本文だけを残す
- `README.md` の文書マップの状態欄を更新する
- 非自明な判断があれば判断ログに記録する（`decisions/YYYY/MM/` に1ファイル＋`DECISIONS.md` 索引に1行）

---

（ここから下に本文を書く）
