# 機能仕様

**状態: 未着手** — この下は本文ではなく、この文書を書くための指示。

## AI への指示（書き終えたらこのセクションごと削除する）

下の項目をユーザーへ聴取し、回答を本文として書く。**推測で埋めない**。ユーザーが答えられない項目は `（未決）` と書いて残し、勝手に決めない。

`MVP_SCOPE.md` に載っている機能を対象にする。載っていない機能をここに書き始めない。

### 聴取する項目

1. どんな画面・機能があるか（まず一覧を作る）
2. 各機能で、何を入力すると何が起きるか
3. 計算・判定のルール（式・条件・単位・端数の扱い）
4. **例外のときどうなるか**（未入力・上限超え・重複・見つからない・通信失敗）
5. 利用者の種類によって見え方・できることが変わるか

### この文書に書かないもの

| 内容 | 行き先 |
|---|---|
| データの保存形式・項目名 | `DATA_MODEL.md` |
| 作る／作らないの線引き | `MVP_SCOPE.md` |
| 使うライブラリ・実装方法 | `TECH_STACK_AND_OPERATIONS.md`（実装の詳細はコード側） |

### 書くときの注意

- **項目4（例外）が最も抜けやすい**。ここが空だと、実装のたびに AI が推測で埋めることになる
- 分量が増えたら機能ごとにセクションを分ける。文書自体を分割するときは `README.md` の文書マップを同じ作業内で更新する

### 書き終えたら

- このセクションと冒頭の状態表示を削除し、本文だけを残す
- `README.md` の文書マップの状態欄を更新する
- 非自明な判断があれば判断ログに記録する（`decisions/YYYY/MM/` に1ファイル＋`DECISIONS.md` 索引に1行）

---

（ここから下に本文を書く）
