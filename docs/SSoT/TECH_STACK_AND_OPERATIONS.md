# 技術構成と運用

**状態: 未着手** — この下は本文ではなく、この文書を書くための指示。

## AI への指示（書き終えたらこのセクションごと削除する）

下の項目をユーザーへ聴取し、回答を本文として書く。**推測で埋めない**。ユーザーが答えられない項目は `（未決）` と書いて残し、勝手に決めない。

構想段階では決められない項目が多い。**その場合は空のままにせず `（未決）` と書いて残す**（空欄だと「決まっていない」のか「聞き忘れ」なのか区別できない）。

### 聴取する項目

1. 何を使って作るか（言語・フレームワーク・DB・主要ライブラリとそのバージョン方針）
2. ログイン・認証をどう扱うか
3. どこで動かすか（ホスティング・実行環境・ドメイン）
4. パスワード・API キー等をどう管理するか（**値そのものはここに書かない**）
5. ローカルで動かすための前提（必要なツール・環境変数の種類）

### この文書に書かないもの

| 内容 | 行き先 |
|---|---|
| テーブル・項目の構造 | `DATA_MODEL.md` |
| 手順書そのもの（セットアップ手順の逐次説明） | `README.md` や `docs/` 配下 |
| **秘密値（パスワード・API キー・トークンの実物）** | **どこにも書かない**（git 履歴に恒久に残る） |

### 書くときの注意

- この文書が扱うのは「何が正しい構成か」であって「どう操作するか」ではない。操作手順を書き始めたら置き場が違う
- 技術を選んだ理由は `decisions/`（索引は `DECISIONS.md`）に書く。ここには選定結果だけを置く

### 書き終えたら

- このセクションと冒頭の状態表示を削除し、本文だけを残す
- `README.md` の文書マップの状態欄を更新する
- 技術選定の理由を判断ログに記録する（`decisions/YYYY/MM/` に1ファイル＋`DECISIONS.md` 索引に1行）

---

（ここから下に本文を書く）
