# MVP定義

**状態: 未着手** — この下は本文ではなく、この文書を書くための指示。

## AI への指示（書き終えたらこのセクションごと削除する）

下の項目をユーザーへ聴取し、回答を本文として書く。**推測で埋めない**。ユーザーが答えられない項目は `（未決）` と書いて残し、勝手に決めない。

この文書は「これは作るんだっけ？」の判定に使う。**判定はこの文書だけを正とする**。

### 聴取する項目

1. 最初のリリースで「これが動けば成立する」と言える最小の範囲は何か
2. そのうち、無いと成立しないものはどれか（優先順位）
3. **恒久的に作らないと決めたものは何か**（理由も一緒に聞く）
4. 何ができたら「MVP は完成した」と判断するか

### この文書に書かないもの

| 内容 | 行き先 |
|---|---|
| 「後で作るかもしれないもの」 | `../work/todo/`（進め方の記録側）。**SSoT は「今どこまで進んだか」を持たない** |
| 「どの順番で作るか」 | `../work/MILESTONES.md` |
| 各機能の詳しい動き | `FUNCTIONAL_SPEC.md` |
| なぜその線引きにしたかの経緯 | `decisions/`（索引は `DECISIONS.md`） |

### 書くときの注意

- **「恒久的に作らない」は理由とセットで残す**。理由が無いと、後から別のセッションが同じ案を「良いアイデア」として再提案してくる
- MVP に入れると決めたものは、それぞれの動きを `FUNCTIONAL_SPEC.md` 側に書く。ここには一覧だけを置く

### 書き終えたら

- このセクションと冒頭の状態表示を削除し、本文だけを残す
- `README.md` の文書マップの状態欄を更新する
- 線引きを決めた理由を判断ログに記録する（`decisions/YYYY/MM/` に1ファイル＋`DECISIONS.md` 索引に1行）

---

（ここから下に本文を書く）
