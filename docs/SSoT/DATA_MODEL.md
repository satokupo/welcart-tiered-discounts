# データモデル

**状態: 未着手** — この下は本文ではなく、この文書を書くための指示。

## AI への指示（書き終えたらこのセクションごと削除する）

下の項目をユーザーへ聴取し、回答を本文として書く。**推測で埋めない**。ユーザーが答えられない項目は `（未決）` と書いて残し、勝手に決めない。

実装が始まると、データに触る作業のすべてがこの文書を参照する。

### 聴取する項目

1. どんな種類の情報を保存するか（一覧）
2. それぞれ何の項目を持つか（必須か任意か、どんな値が入るか）
3. 情報同士がどうつながるか（どれがどれに紐づくか）
4. 誰がどこまで見られるか・変更できるか
5. 一度検討してやめた構造があるか（あれば理由とセットで記録する）

### この文書に書かないもの

| 内容 | 行き先 |
|---|---|
| 画面上の動き・入力の流れ | `FUNCTIONAL_SPEC.md` |
| どの DB 製品を使うか | `TECH_STACK_AND_OPERATIONS.md` |
| 変更の経緯 | `decisions/`（索引は `DECISIONS.md`） |

### 書くときの注意

- **項目5（やめた構造）を残すと、後から同じ案が再提案されるのを防げる**。「採用しない」と理由を明記しておく
- 値の入れ方（ロジック）が未実装でも、構造（項目・型・つながり）は決まった時点で書く。構造と実装を混ぜて先送りしない

### 書き終えたら

- このセクションと冒頭の状態表示を削除し、本文だけを残す
- `README.md` の文書マップの状態欄を更新する
- 構造を決めた理由を判断ログに記録する（`decisions/YYYY/MM/` に1ファイル＋`DECISIONS.md` 索引に1行）

---

（ここから下に本文を書く）
