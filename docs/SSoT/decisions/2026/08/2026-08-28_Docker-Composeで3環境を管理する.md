# 2026-08-28: Docker Compose で 3 環境を管理する

- **決定**: Docker Compose の YAML で `recommended`、`latest`、`minimum` の 3 環境を分離して管理し、`recommended` を最初に構築する。開発作業時間の計測は、環境準備の完了条件を満たした後に開始する。
- **理由**: 主たる対応環境で再現可能な開発基盤を先に確立しつつ、最新環境と公称下限環境を別の証拠として確認し、環境準備と課題実装の時間を混同しないため。
